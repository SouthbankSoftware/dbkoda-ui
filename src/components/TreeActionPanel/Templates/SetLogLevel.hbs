db.getSiblingDB("admin").runCommand(
  {setParameter: 1,  
   logComponentVerbosity: 
     {
        verbosity: {{{verbosity}}},{{#each Components}}
       "{{{Component}}}":{verbosity: {{{Level}}} } {{#unless last}},{{/unless}} {{/each}}
     }
  });
 