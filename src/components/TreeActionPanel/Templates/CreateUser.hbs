db.getSiblingDB("{{Database}}").createUser({ 
   user: "{{UserName}}",
   pwd: "{{Password}}", {{#if CustomData}} 
   customData:{{{CustomData}}}, {{/if}}
     roles: [ {{#each Roles}} 
             {role:"{{Role}}", db: "{{Database}}" } {{#unless last}},{{/unless}}    {{/each}}
            ]              
    },          
    {  w: "majority" }
);
 