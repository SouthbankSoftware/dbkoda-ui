var insResult={};

insResult=db.getSiblingDB("{{{Database}}}").getCollection("{{{CollectionName}}}").
   insert(
     {{{DocumentArray}}} ,
     { ordered: {{{Ordered}}}
     });

printjson(insResult);
