db.getSiblingDB("{{{Database}}}").getCollection("{{{CollectionName}}}").createIndex(
  { {{#each Keys}}
     "{{{AttributeName}}}":{{{Direction}}}{{#unless last}},{{/unless}}{{/each}} } ,
  {
    {{#if IndexName}}
    name: "{{{IndexName}}}",{{/if}}background: {{{Background}}},
    unique: {{{Unique}}},{{#if partialFilterExpression}}
    partialFilterExpression: {{{partialFilterExpression}}},{{/if}}{{#if expireAfterSeconds}}
    expireAfterSeconds :  {{{expireAfterSeconds}}} , {{/if}}
    sparse: {{{Sparse}}}
  }
);