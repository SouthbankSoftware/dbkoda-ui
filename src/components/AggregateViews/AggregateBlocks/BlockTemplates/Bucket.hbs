{
  $bucket: {
      groupBy: {{{groupBy}}}, 
      {{#if defaultValue}} default: {{{defaultValue}}} , 
      {{/if ~}}
      {{#if outputVars}}  output: {
        {{#each outputVars}} {{varName}} :{{{varExpression}}} {{#unless last}},{{/unless}}{{/each}},
      }, 
      {{/if ~}}
      boundaries: [ {{#each lowerBounds}} {{lowerBound}}  {{#unless last}},{{/unless}}{{/each}} ]
   }
}