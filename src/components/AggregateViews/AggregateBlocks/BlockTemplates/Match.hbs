  { $match:{
  {{#if UseOr ~}} "$or":[
          {{#if FilterKeys}}
               {{#each FilterKeys}}
      { "{{{AttributeName}}}":{ {{{Operator}}}:{{{Value}}} } }
              {{~#unless last}},{{/unless}}
               {{/each}}
    ]
             {{/if~}}
       {{else if UseAnd}} "$and":[
          {{#if FilterKeys}}
               {{#each FilterKeys}}
      { "{{{AttributeName}}}":{ {{{Operator}}}:{{{Value}}} } }
              {{~#unless last}},{{/unless}}
               {{/each}}
    ]
        {{/if~}} 
        {{else ~}}   
          {{#if FilterKeys ~}}
            {{#each FilterKeys}}
      "{{{AttributeName}}}":{ {{{Operator}}}:{{{Value}}} }
              {{~#unless last}},{{/unless}}
            {{/each~}}
          {{/if~}}
       {{/if}}
   }
  }
